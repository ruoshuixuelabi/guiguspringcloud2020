server:
  port: 3377
spring:
  application:
    name: nacos-config-consumer
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
#        group: dev
#        namespace:
management:
  endpoints:
    web:
      exposure:
        include: '*'